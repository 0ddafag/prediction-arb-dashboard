{
  "name": "prediction-arb-dashboard",
  "private": false,
  "version": "0.1.0",
  "description": "MVP dashboard for Polymarket ↔ bookmaker comparison with screenshot/manual intake and live calculator.",
  "main": "server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node server.js",
    "start": "playwright install chromium && node server.js",
    "test": "node --test",
    "coverage:report": "node scripts/coverage-report.js",
    "venues:probe": "node scripts/probe-prediction-venues.js",
    "fonbet:probe": "node scripts/probe-fonbet.js",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "1.52.0"
  }
}
